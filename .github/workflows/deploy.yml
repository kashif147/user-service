name: Deploy User Service

on:
  push:
    branches:
      - gateway
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Sync code to VM
        run: |
          # Never use rsync --delete-excluded: excluded env files must remain on the VM.
          rsync -avz --delete \
          --include '.env.example' \
          --include '.env.sample' \
          --exclude '.env' \
          --exclude '.env.*' \
          --exclude '.env_*' \
          --exclude '*.env' \
          --exclude '.envrc' \
          -e "ssh -o StrictHostKeyChecking=no" ./ \
          ${{ secrets.VM_USER }}@${{ secrets.VM_HOST }}:/home/deploy/membership-platform/user-service/
      - name: Build and restart user-service
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.VM_USER }}@${{ secrets.VM_HOST }} << 'EOF'
            cd /home/deploy/membership-platform/user-service
            docker compose build user-service
            docker compose up -d user-service
          EOF
